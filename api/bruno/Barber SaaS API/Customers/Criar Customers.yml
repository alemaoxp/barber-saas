info:
  name: Criar Customers
  type: http
  seq: 1

http:
  method: POST
  url: POST    http://localhost:8080/api/v1/customers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
