info:
  name: Cancelar Interesse
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:8080/api/v1/customers/{{customerId}}/availability-interests/{{interestId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
